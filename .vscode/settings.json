{
    "sqltools.useNodeRuntime": true,
    "sqltools.connections": [
        {
            "previewLimit": 50,
            "driver": "SQLite",
            "name": "db",
            "database": "${workspaceFolder:mario-fitness}/databases/test.db"
        },
        {
            "previewLimit": 50,
            "driver": "SQLite",
            "name": "dbroot",
            "database": "${workspaceFolder:mario-fitness}/test.db"
        }
    ]
}